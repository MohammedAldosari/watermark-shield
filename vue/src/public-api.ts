/*
 * Public API of `watermark-shield/vue`.
 *
 * Vue 3 binding for `watermark-shield`. Pulls in `vue` as a peer
 * dependency — install only when you use this entry point.
 */
export { useWatermarkShield } from './lib/use-watermark-shield';
export type { WatermarkShieldHandle } from './lib/use-watermark-shield';
