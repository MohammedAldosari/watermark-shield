/*
 * Public API of `watermark-shield/angular`.
 *
 * Angular bindings for `watermark-shield`. Pulls in `@angular/core` and
 * `@angular/common` as peer dependencies — install only when you use
 * this entry point.
 */
export { WatermarkShieldService } from './lib/watermark-shield.service';
export { WatermarkShieldDirective } from './lib/watermark-shield.directive';
